// app/api/chatbot/chatArea/route.ts
// directly used Socket IO in app/home/cemtemChat/page.tsx for real time communication with python-ai-agent

import { NextRequest, NextResponse } from 'next/server';

export async function GET(req: NextRequest) {
        return NextResponse;
}